# 示例项目治理规则

本文件仅用于说明 v6 policy snapshot 和治理敏感路径的工作方式。

- 业务修改必须落在已批准的 `scope-contract.yaml` 路径内。
- `high_risk` 任务必须具备独立审查、目标测试和回滚方案。
- 验证证据必须绑定当前代码版本或明确标记工作区摘要。
- 修改 `AGENTS.md`、`.agents/**` 或治理 CI 时，必须作为独立治理变更审批。
